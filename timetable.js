// scripts.js
document.getElementById('term-select').addEventListener('change', function() {
    const selectedTerm = this.value;
    // Add logic to update the timetable based on the selected term
    console.log('Selected term:', selectedTerm);
});
